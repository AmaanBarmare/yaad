# Base URL of the deployed FastAPI backend (no trailing slash).
# Local dev defaults to http://localhost:8000 if unset.
# On Vercel, set this to your backend deployment URL, e.g.
#   https://yaad-api.vercel.app
VITE_API_BASE=http://localhost:8000
